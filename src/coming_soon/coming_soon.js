/**
 * Created by wujingshi on 2017/9/24.
 */
(function(angular){
    var app=angular.module("douban_coming_soon",["ngRoute","common"]);
    app.config(["$routeProvider",function($routeProvider){
        $routeProvider.when("/coming_soon/:page?",{
            templateUrl:"./coming_soon/coming_soon.html",
            controller:"comingMenuCon"
        })
    }]);

    app.controller("comingMenuCon",["$scope","$route","serjsonp","$routeParams","$window",function($scope,$route,serjsonp,$routeParams,$window){
        //定义每一页的容量
        $scope.pageSize=10;
        //定义页码
        $scope.pageIndex=+($routeParams.page||"1");
        //调取数据
        serjsonp({
            url:"http://api.douban.com/v2/movie/coming_soon",
            params:{
                count:$scope.pageSize,
                start:($scope.pageIndex-1)*$scope.pageSize
            },
            callback:function(data){
                $scope.movie=data;
                $scope.pageCount=$window.Math.ceil(data.total/$scope.pageSize);
                $scope.$apply();
            }
        });
        $scope.getPage=function(pageIndex){
            if(pageIndex<1||pageIndex>$scope.pageCount){
                return;
            }
            $route.updateParams({
                page:pageIndex
            });
        }
    }])
})(angular)